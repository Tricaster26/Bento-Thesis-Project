{"name": "browser", "code": "\nimport urllib.request\nimport hashlib\nimport cacheout\nimport zlib\nimport os\n\ndef browser(url, padding):\n    list = None\n    if(os.path.exists('./cache')):\n        list = os.listdir('./cache')\n    else:\n        os.mkdir('./cache')\n    body= urllib.request.urlopen(url, timeout=1).read()\n    compressed= zlib.compress(list)\n    text = \"hello\"\n    api.send(text)\n\n"}